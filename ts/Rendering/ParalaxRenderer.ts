import Renderer from './Renderer';
import ParalaxObject from './Sprites/ParalaxObject';

/** Positions sprites with some some offset */
export default class ParalaxRenderer extends Renderer<ParalaxObject>
{
    /** Render the sprites in a 'paralax' way */
    public render(): void
    {
        //
    }
}
